import { Container } from "components/Container"
import { NextPage } from "next"

interface ProjectProps {}

const Project: NextPage<ProjectProps> = ({}) => {
  return (
    <Container>
      <div>indiv project page</div>
    </Container>
  )
}

export default Project
